# Core data pipeline
pandas>=2.0
numpy>=1.24
requests>=2.31

# Deduplication (Agents 2, 7)
fuzzywuzzy>=0.18
python-Levenshtein>=0.21   # speeds up fuzzywuzzy significantly

# Neo4j graph database (Scripts 20, 21, data/etl/)
neo4j>=5.0

# OpenAI LLM (Agents 4, 10, 11)
openai>=1.0

# Scraping APIs (Agent 1, 8)
outscraper>=2.1
google-search-results>=2.4   # provides `from serpapi import GoogleSearch`
apify-client>=1.6
